// import axios from 'axios';

// describe('HTTP Server Check', () => {
//   it('should check if the server is running by accessing http://localhost:3000/docs', async () => {
//     const checkUrl = 'http://localhost:3000/docs';
//     try {
//       await axios.get(checkUrl); // Use GET as requested
//       console.log(`Server is responding at ${checkUrl}`);
//     } catch (error) {
//       console.error(`Server is not responding at ${checkUrl}. Tests cannot proceed.`);
//       process.exit(1);
//     }
//   });
// });
